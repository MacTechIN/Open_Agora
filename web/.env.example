# Postgres 연결 문자열 (Neon 콘솔 → Connect 에서 복사)
#
# 비밀번호가 포함된 접속 정보다. 저장소에 커밋하지 않는다.
# 로컬에서 쓰려면 이 파일을 .env.local 로 복사해 값을 채운다.
DATABASE_URL=postgresql://user:password@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require
