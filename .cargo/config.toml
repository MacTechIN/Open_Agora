# Windows 빌드는 CRT를 정적 링크한다.
#
# 기본값(동적 링크)으로 만든 cdylib는 VCRUNTIME140.dll을 요구한다. VC++
# 재배포 패키지가 없는 PC에서는 DLL 로드가 실패하고, 그 호출이 창 생성 전에
# 일어나면 창도 오류창도 없이 앱이 죽는다. "설치 없이 실행"을 표방하면서
# 시스템 런타임을 전제할 수는 없다.
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]
